import React from 'react';

// LoadingOverlay is intentionally empty as the loading state is now entirely 
// handled by the immersive WelcomeAnimation signature trace.
const LoadingOverlay = () => {
  return null;
};

export default LoadingOverlay;